mixin button(id, icon, text, color, baseClass, tooltip, tooltipPlacement, dontHideText)
	a.btn(
			class=(color ? 'btn-' + color : 'btn-primary') + ' ' + (baseClass ? baseClass : ''),
			id=(id ? id : ''),
			data-toggle='tooltip',
			data-placement=(tooltipPlacement ? tooltipPlacement : 'top'),
			title=(tooltip ? tooltip : '')
		)
		i.fa(class='fa-' + icon)
		if text && (!tooltip || dontHideText)
			span &nbsp;
			span.text= text
	|  

mixin modal(id, title, text, cancelButton, deleteButton, deleteButtonId)
	div.modal.fade(id=id)
		div.modal-dialog
			div.modal-content
				div.modal-header
					button.close(type='button', data-dismiss='modal', aria-label='Close')
					h4.modal-title= title
				div.modal-body
					if text
						p= text
					if block
						block
				div.modal-footer
					button.btn.btn-primary(type='button', data-dismiss='modal')= cancelButton
					if deleteButton
						button.btn.btn-danger(type='button', data-dismiss='modal', id=deleteButtonId)= deleteButton

html
	head
		link(rel='stylesheet' href='css.min.css')
	body
		+modal('shortcut-help-modal', 'Shortcuts', '', 'OK')
			p You can use some shortcuts to save time. The following keys are supported right now:
			ul
				li
					kbd Space
					|  
					span Play/Pause
				li
					kbd 1-9
					|  
					span Seek to part N of the audio file
				li
					kbd
						i.fa.fa-arrow-left
					|  
					span Skip back 5 seconds
				li
					kbd
						i.fa.fa-arrow-up
					|  
					span Skip back 10 seconds
				li
					kbd
						i.fa.fa-arrow-right
					|  
					span Skip forward 5 seconds
				li
					kbd
						i.fa.fa-arrow-down
					|  
					span Skip forward 10 seconds



		div.container
		nav.navbar.navbar-light.bg-faded#main-navbar
			div
				a.navbar-brand(href='https://github.com/blaues0cke/PiSleepTalk', target='_blank') PiSleepTalk

				ul.nav.navbar-nav
					// Thanks to
					// * http://stackoverflow.com/questions/14144274/jade-conditional-if-else-to-add-class-to-div-inline
					li.nav-item(class=!pageData || !pageData.context || pageData.context == 'home' ? 'active' : '')
						a(href='/').nav-link Home
					li.nav-item(class=pageData && pageData.context && pageData.context == 'videos'  ? 'active' : '')
						a(href='/videos').nav-link Videos
					li.nav-item(class=pageData && pageData.context && pageData.context == 'status'  ? 'active' : '')
						a(href='/status').nav-link Status

			div.pull-right
				div#info-area
				+button('shortcut-help', 'question', 'Help', 'primary', '', 'Help', 'left')

		block content
		script(src='js.min.js')